input {
  http {
    host => "0.0.0.0"
    port => 8080
    codec => json
  }
}

filter {
  json {
    source => "event.original"
    target => "parsed_json"
  }
}
output {
  stdout {
    codec => rubydebug
  }
}
